start:
  args:
  - abicodec
  - apiproxy
  - blockmeta
  - booter
  - dashboard
  - dgraphql
  - eosq
  - eosws
  - fluxdb
  - merger
  - mindreader
  - node-manager
  - relayer
  - search-archive
  - search-indexer
  - search-live
  - search-router
  - trxdb-loader
  flags:
    # Working only once feature/filtering is merged in!
    # common-include-filter-expr: "*"
    # common-exclude-filter-expr: (receiver == "eosio" && action == "onblock")
