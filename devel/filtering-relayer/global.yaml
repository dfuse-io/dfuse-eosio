start:
  args:
  - booter
  - merger
  - mindreader
  - node-manager
  - relayer
  flags:
    # Does not work due to a bug, having it in the CLI flag directly for now ...
    # relayer-grpc-listen-addr: :10000
