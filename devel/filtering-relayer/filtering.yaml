start:
  args:
  - abicodec
  - apiproxy
  - blockmeta
  - dashboard
  - dgraphql
  - eosq
  - eosws
  - fluxdb
  - relayer
  - search-archive
  - search-indexer
  - search-live
  - search-router
  - trxdb-loader
  flags:
    # Does not work due to a bug, having it in the CLI flag directly for now ...
    #relayer-source: ":10000"
    #common-include-filter-expr: receiver == "eosio.token"
